---
import SVG from "./SVG.astro";

const services = [
  { title: "Graphic Design", icon: "pattern-graphic-design", color: "blue" },
  { title: "UI/UX", icon: "pattern-ui-ux", color: "yellow" },
  { title: "Apps", icon: "pattern-apps", color: "pink" },
  { title: "Illustrations", icon: "pattern-illustrations", color: "primary" },
  { title: "Photography", icon: "pattern-photography", color: "cyan" },
  {
    title: "Motion Graphics",
    icon: "pattern-motion-graphics",
    color: "purple",
  },
];
---

<section class="hero">
  <div class="container">
    <div class="hero__text">
      <h1>Design solutions made easy</h1>
      <p>
        With over ten years of experience in various design disciplines, I’m
        your one-stop shop for your design needs.
      </p>
    </div>
    <div class="hero__services">
      <h2 aria-hidden="true">Services</h2>
      <ul class="services-list">
        {
          services.map((service) => (
            <li class:list={["services-item", `bg-${service.color}`]}>
              <div class="icon">
                <SVG name={service.title} />
              </div>
              <h3>{service.title}</h3>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</section>

<style lang="scss">
  $hero-spacing: clamp(2.5rem, -0.3571rem + 5.9524vw, 5rem);

  .hero__services {
    h2 {
      @include visually-hidden;
    }
  }

  .hero {
    padding-top: $hero-spacing;

    &__text {
      @include flex-center;
      flex-direction: column;
      gap: 1.25rem;
      text-align: center;
    }
  }

  .services {
    &-list {
      align-items: stretch;
      display: grid;
      gap: 1.25rem;
      grid-template-columns: repeat(2, 1fr);
    }

    &-item {
      border-radius: 8px;
      padding: 1.25rem;

      h3 {
        color: var(--color-body-text-light);
      }

      &:nth-child(1) {
        grid-column: 1 / span 2;
        grid-row: 1 / span 2;
      }

      &:nth-child(4),
      &:nth-child(5),
      &:nth-child(6) {
        grid-column: 1 / span 2;
      }
    }
  }

  .bg- {
    &blue {
      background-color: var(--color-blue);
    }
    &purple {
      background-color: var(--color-purple);
    }
    &cyan {
      background-color: var(--color-cyan);
    }
    &pink {
      background-color: var(--color-pink);
    }
    &yellow {
      background-color: var(--color-yellow);
    }

    &primary {
      background-color: var(--color-primary);
    }
  }

  .container {
    display: grid;
    gap: $hero-spacing;
  }
</style>
